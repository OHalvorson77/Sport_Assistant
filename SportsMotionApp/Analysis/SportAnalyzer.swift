import Foundation

class SportsAnalyzer {
    static let shared = SportsAnalyzer()

    func analyze(pose: String) {
        // This is where you'd evaluate the movement and update feedback
        print("Analyzing pose: \(pose)")
        // e.g., notify feedback system
    }
}
